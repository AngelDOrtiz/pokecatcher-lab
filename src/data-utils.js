import pokeData from './data.js';
import { encounterPokemon } from './local-storage-utils.js';

export function presentThreePokemon() {
    let randomNumber1 =
}

function getRandomIndex() {
    return Math.floor(Math.random() * pokeData.length);
}